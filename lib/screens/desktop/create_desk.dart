import 'package:flutter/material.dart';

class CreateDesk extends StatelessWidget {
  const CreateDesk({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Create Desktop')),
    );
  }
}
